# rip

A Rust TUI to list processes bound to ports and kill them interactively.

## Usage

```
cargo run
```

## Controls

- `↑/k` `↓/j` - Navigate
- `Enter/d` - Kill process
- `r` - Refresh
- `q` - Quit
